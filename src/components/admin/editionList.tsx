import React, {useState} from 'react';
import {lstEdition} from "../../interface/admin";

function EditionList(props:lstEdition) {
    const [lstElem,setLstElem] = useState<any[]>([])
    return (
        <div>

        </div>
    );
}

export default EditionList;